# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MedicationSchedule' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  	pod 'SQLite.swift', '~> 0.12.0'
  # Pods for MedicationSchedule

  target 'MedicationScheduleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MedicationScheduleUITests' do
    # Pods for testing
  end

end
